# CI validation

This marker commit exists to trigger the repository pull-request test workflow after enabling `workflow_dispatch` on `main`.

The security branch must not be merged until type-checking and the complete test suite pass in GitHub Actions.
